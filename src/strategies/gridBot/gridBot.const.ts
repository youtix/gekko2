/** Maximum total open orders allowed at once (both sides combined). */
export const INTERNAL_OPEN_ORDER_CAP = 50;
/** Default retry attempts for order create/cancel failures. */
export const DEFAULT_RETRY_LIMIT = 3;
/** Default drift tolerance (%) before triggering a rebalance. */
export const DEFAULT_REBALANCE_TOLERANCE_PERCENT = 2;

export const DEFAULT_AMOUNT_ROUNDING = 8;
