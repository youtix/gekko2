watch:
  asset: BTC
  currency: USDT
  mode: realtime
  timeframe: '1d'
  warmup:
    candleCount: 365

exchange:
  name: binance

storage:
  type: sqlite
  database: ./db/binance-BTC_USDT.sql

plugins:
  - name: CandleWriter
